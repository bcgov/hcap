const reportType = {
  HIRED: 'milestone-hired',
  ROS: 'milestone-ros',
  TEST: 'milestone-test',
};

const DEFAULT_REGION_NAME = '';
const DEFAULT_STATUS = 'N/A';

module.exports = {
  reportType,
  DEFAULT_REGION_NAME,
  DEFAULT_STATUS,
};
